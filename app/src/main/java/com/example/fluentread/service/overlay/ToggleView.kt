package com.example.fluentread.service.overlay

import android.content.Context
import android.graphics.Point

class ToggleView(
    context: Context, startPoint: Point
): ToggleControl(context, ToggleLayout(context)) {
}